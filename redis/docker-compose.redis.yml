services:
  redis:
    image: redis/redis-stack:latest
    container_name: redis-track-stack
    ports:
      - "6379:6379"
      - "8081:8001"   # RedisInsight UI (host 8081 — Baymax FRONT agent uses 8001)
    restart: unless-stopped
